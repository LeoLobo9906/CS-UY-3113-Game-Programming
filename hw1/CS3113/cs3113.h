#ifndef CS3113_H
#define CS3113_H

#include "raylib.h"
#include <stdio.h>

// Lecture 3 notes include this enum in the library header
enum AppStatus { TERMINATED, RUNNING };

Color ColorFromHex(const char *hex);

#endif
